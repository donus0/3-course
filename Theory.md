# Теория

Курс: **вероятностные и стохастические модели анализа данных**.

- **ЛР №1** — математическая статистика (`compute.py`)
- **ЛР №2** — теория чисел, тесты простоты, ГПСЧ (`vismad.py`)

---

# ЛР №1. Математическая статистика

## Общие обозначения (ЛР №1)

| Символ | Смысл |
|--------|--------|
| **n** | объём выборки (число значений x₁, x₂, …, xₙ) |
| **N** | номер студента в списке группы (с какой **строки** таблицы начинать) |
| **R** | число строк в исходной таблице |
| **xᵢ** | i-е значение выборки |
| **xₘᵢₙ**, **xₘₐₓ** | минимум и максимум выборки |
| **Rₓ** | размах вариации |
| **k** | число интервалов группировки |
| **h** | длина (шаг) интервала |
| **α₀ … αₖ** | границы интервалов |
| **nᵢ** | абсолютная частота i-го интервала |
| **x̄**, **s²** | выборочное среднее и исправленная дисперсия |

---

## Пункт 1. Формирование выборки

**Задание:** взять **10 строк** исходной таблицы, начиная с **N-й** строки (по списку группы). В каждой строке по 10 чисел → всего **n = 100** значений.

**Порядок:** значения идут подряд: сначала все числа из 1-й взятой строки, затем из 2-й, …, из 10-й.

**Если N > R:** в программе используется циклический переход по таблице (остаток от деления N на R); строки после конца таблицы «переходят» снова к началу.

**Зачем:** получить одну конкретную реализацию выборки для всех дальнейших расчётов.

---

## Пункт 2. Минимум, максимум, размах

```
xₘᵢₙ = min(xᵢ)
xₘₐₓ = max(xᵢ)
Rₓ = xₘₐₓ − xₘᵢₙ
```

**Размах** Rₓ показывает, насколько «растянуты» данные по оси значений. Он нужен для построения интервалов группировки.

---

## Пункт 3. Число интервалов k

По объёму выборки **n**:

```
k = целая часть от ( 1 + 3,32 · lg n )
```

где **lg n** — десятичный логарифм (log₁₀ n).

**Смысл:** эмпирическое правило — чем больше n, тем больше интервалов, но не слишком много, чтобы в каждом интервале были точки.

**Пример:** n = 100, lg 100 = 2, выражение 1 + 3,32·2 = 7,64, значит **k = 7**.

---

## Пункт 4. Шаг интервала h

```
        Rₓ
h = ───────
         k
```

**Округление:** h округляют до заданной точности **ε** (в ТЗ ε = 0,01, т.е. два знака после запятой). В программе число знаков задаётся параметром ε.

**Смысл:** все интервалы (кроме возможной подгонки последней границы) имеют одинаковую ширину h.

---

## Пункт 5. Границы интервалов αᵢ

```
α₀ = xₘᵢₙ − h/2
α₁ = α₀ + h
α₂ = α₁ + h
  …
αᵢ = α₀ + i·h   (i = 0, 1, …, k)
```

**Контроль из ТЗ:** αₖ ≈ xₘₐₓ и (αₖ − α₀) ≈ k·h (близко к размаху Rₓ).

Если αₖ < xₘₐₓ, последнюю границу поднимают до xₘₐₓ, чтобы все точки попали в таблицу.

**Интервалы для группировки:** (α₀; α₁], (α₁; α₂], …, (αₖ₋₁; αₖ].

---

## Пункт 6. Изображение на числовой прямой

На оси **Ox** отмечают:

- все значения выборки (точки);
- вертикали (или деления) в точках α₀, α₁, …, αₖ.

**Правило из ТЗ:** если значение попадает **ровно на границу** между интервалами, его относят к **правому** интервалу.

Формально:

- для первого интервала: **α₀ ≤ x ≤ α₁**;
- для остальных: **αᵢ₋₁ < x ≤ αᵢ** при i ≥ 1.

Так каждая точка попадает ровно в один интервал.

---

## Пункт 7. Статистическая таблица

Для каждого интервала **i = 1, 2, …, k**:

| Столбец | Формула / смысл |
|---------|------------------|
| № | номер интервала |
| Интервал | полуинтервал (αᵢ₋₁; αᵢ] |
| Середина | x̄ᵢ = (αᵢ₋₁ + αᵢ) / 2 |
| nᵢ | сколько точек выборки попало в интервал |
| nᵢ/n | относительная частота |
| Nᵢ | накопленная частота: Nᵢ = n₁ + n₂ + … + nᵢ |

**Проверка:** n₁ + n₂ + … + nₖ = n.

**Гистограмма и ЭФР** (в программе на том же экране): по исходной выборке строят столбчатую диаграмму с теми же границами αᵢ и ступенчатую эмпирическую функцию распределения:

```
         (число xⱼ, для которых xⱼ ≤ x)
Fₙ(x) = ───────────────────────────────
                    n
```

---

## Пункт 8. Среднее и исправленная дисперсия (сгруппированная выборка)

Середины интервалов **x̄ᵢ** и частоты **nᵢ** используют как «представителей» данных в интервале.

**Выборочное среднее (сгруппированное):**

```
         1     k
x̄* = ─── ·  Σ   nᵢ · x̄ᵢ
         n    i=1
```

**Исправленная дисперсия (сгруппированная):**

```
           1     k
s*² = ───────── ·  Σ   nᵢ · (x̄ᵢ − x̄*)²
         n − 1   i=1
```

Для сравнения по **исходной** (несгруппированной) выборке:

```
         1     n
x̄ = ─── ·  Σ   xⱼ
         n    j=1

           1     n
s² = ───────── ·  Σ   (xⱼ − x̄)²
         n − 1   j=1
```

Сгруппированные оценки приближённые; при укрупнении интервалов они могут заметно отличаться от x̄ и s² по полной выборке.

---

## Пункт 9. Проверка нормальности

**Гипотеза H₀:** случайная величина X имеет **нормальное распределение** N(μ, σ²).

**Критерий согласия Пирсона (χ²):**

1. Оценить μ̂ = x̄, σ̂ — исправленное СКО по выборке.
2. Разбить ось на те же интервалы (αᵢ₋₁; αᵢ], посчитать наблюдаемые nᵢ.
3. Для нормального закона с параметрами μ̂, σ̂ найти **ожидаемые** частоты Eᵢ = n · pᵢ, где pᵢ = F(αᵢ) − F(αᵢ₋₁), F — функция распределения N(μ̂, σ̂²).
4. Если какое-то Eᵢ < 5, интервалы **объединяют** с соседними (требование применимости χ²).
5. Статистика:

```
χ² =  Σ   (nᵢ − Eᵢ)² / Eᵢ
```

6. Число степеней свободы: **r − 1 − s**, где r — число интервалов после объединения, **s = 2** (оценили μ и σ).
7. По таблице χ² или по **p-value**: при уровне 0,05 если p-value < 0,05, **H₀ отвергают**.

---

## Пункт 10. Проверка H₀: σ² = 1

**Гипотеза:** дисперсия генеральной совокупности равна σ₀² = 1.

**Уровень значимости:** α = 0,05 (двусторонняя проверка).

Исправленная выборочная дисперсия **s²**. При H₀ и нормальности выборки статистика

```
        (n − 1) · s²
χ² = ─────────────────
           σ₀²
```

имеет распределение χ² с **(n − 1)** степенями свободы.

**Критическая область (двусторонняя):** отвергают H₀, если χ² **не попадает** в доверительный интервал между квантилями χ²₀,₀₂₅(n−1) и χ²₀,₉₇₅(n−1).

В программе σ₀² = 1.

---

## Пункт 11. Проверка H₀: M(X) = 0

**Гипотеза:** математическое ожидание **μ = 0**.

**Уровень значимости:** α = 0,05.

При нормальности и неизвестной дисперсии используют **одновыборочный t-критерий Стьюдента**:

```
        x̄ − 0
t = ───────────
       s / √n
```

где **s** — исправленное СКО (s = √(s²)). При H₀ статистика имеет распределение Стьюдента с **(n − 1)** степенями свободы.

**Решение:** сравнивают **p-value** с 0,05: если p-value < 0,05, **H₀ отвергают** (среднее статистически значимо отличается от 0).

---

## Связь пунктов ЛР №1 с `compute.py`

| П. ТЗ | Функции |
|-------|---------|
| 1 | `build_sample_array`, `sample_row_numbers`, `resolve_start_row_index` |
| 2 | `min_max_range` → в `analyze` |
| 3 | `count_intervals` |
| 4 | `step_h` |
| 5 | `interval_bounds` |
| 6 | `which_interval` (правило границы) |
| 7 | `frequencies`, `IntervalBin`, `analyze` |
| 8 | `grouped_mean`, `grouped_variance`, `sample_mean`, `sample_variance` |
| 9 | `chi2_normality_gof` |
| 10 | `variance_test_sigma2` |
| 11 | `mean_test_zero` |

Полный расчёт для заданий 2–8: один вызов `analyze(x)`.

---

## Исходные данные из ТЗ

В задании приведена таблица из 20 строк по 10 чисел (для примера с **N** по списку). В приложении те же данные загружаются из файла датасета по умолчанию.

---

# ЛР №2. ВиСМАД (Р1)

## Общие обозначения (ЛР №2)

| Символ | Смысл |
|--------|--------|
| **ℕ, ℤ** | натуральные и целые числа |
| **gcd(a, b)** | наибольший общий делитель |
| **a mod m** | остаток от деления a на m |
| **φ(n)** | функция Эйлера |
| **ℙ** | множество простых чисел |
| **(a/p), (a/n)** | символы Лежандра и Якоби |
| **H₀** | нулевая гипотеза («n простое») |
| **a** | свидетель (основание теста), 2 ≤ a ≤ n−2 |
| **Xᵢ** | состояние генератора на шаге i |
| **P** | случайная величина — период генератора при случайных параметрах |
| **D(P)** | дисперсия P (в программе — выборочная по серии опытов) |
| **ℤ₂ᵏ** | целые 0 … 2ᵏ−1 (для случайного выбора параметров в опытах) |

---

## Пункт 1. Функция Эйлера φ(n)

**Определение.** φ(n) — число натуральных k из {1, 2, …, n}, для которых gcd(k, n) = 1.

**По определению:**

```
φ(n) = |{ k ∈ {1,…,n} : gcd(k, n) = 1 }|
```

**Основная теорема арифметики.** n = p₁^e₁ · … · pᵣ^eᵣ (pᵢ — различные простые). Тогда

```
φ(n) = n · ∏ (1 − 1/pᵢ)
```

Факторизацию n выполняют **самостоятельно** (деление на простые до √n).

**Через дискретное преобразование (формула из ТЗ):**

```
         n
φ(n) =  Σ   gcd(k, n) · cos(2πk / n)
        k=1
```

Три способа в программе должны давать одно и то же целое φ(n) (ДПФ — с погрешностью округления).

---

## Пункт 2. Тест простоты Ферма

**Идея.** Для простого n и a, не делящего n: **малая теорема Ферма**

```
a^(n−1) ≡ 1 (mod n)
```

**H₀:** n — простое. **Проверка:** для каждого свидетеля a

```
если a^(n−1) mod n ≠ 1  →  n составное (H₀ отвергнута)
```

Если ни один свидетель не «поймал» составность — n **вероятно простое** (тест может ошибаться на числах Кармайкла).

**Требование ТЗ:** все свидетели a **попарно различны**, 2 ≤ a ≤ n−2.

---

## Пункт 3. Лежандр, Якоби, Соловей–Штрассен

**Символ Лежандра (a/p),** p — нечётное простое, gcd(a, p) = 1:

```
(a/p) =  1,  если ∃ x: x² ≡ a (mod p)
         −1, если такого x нет
          0,  если p | a
```

На практике: (a/p) ≡ a^((p−1)/2) (mod p), результат приводят к {−1, 0, 1}.

**Символ Якоби (a/n),** n — нечётное, gcd(a, n) = 1: определяется **мультипликативно** по разложению n на простые множители (произведение символов Лежандра). Вычисляется без полной факторизации n (алгоритм похож на Euclidean).

**Тест Соловея–Штрассена (вероятностный).** Для нечётного n > 2 и свидетеля a, gcd(a, n) = 1:

```
(a/n) ≡ a^((n−1)/2)  (mod n)
```

Если не выполняется — n **составное**. Иначе — **вероятно простое**. Свидетели a — **различные**.

---

## Пункт 4. Тест Миллера–Рабина

**Подготовка.** n − 1 = 2ˢ · d, где d нечётно.

**H₀:** n простое. Для свидетеля a (2 ≤ a ≤ n−2) положить x = aᵈ mod n.

- если x = 1 или x = n−1 — a не свидетельствует о составности;
- иначе повторять x = x² mod n не более s−1 раз: если ни разу x ≠ n−1 — n **составное**.

Если ни один из различных свидетелей не выявил составность — n **вероятно простое** (для практических n — надёжнее теста Ферма).

---

## Пункт 5. Линейный конгруэнтный генератор (LCG)

**Рекурренция:**

```
Xₙ₊₁ = (a · Xₙ + b) mod m
```

**Период** — длина цикла состояний до первого повторения (при фиксированных a, b, m и начальном X₀).

**Опыт из ТЗ.** Параметры a, b, m берут из **ℤ₂³²**; для оценки распределения периода случайно сэмплируют параметры из **ℤ₂¹²** (в программе — целые 0 … 2¹²−1). Для каждого набора считают период Pᵢ; **D(P)** — дисперсия этой выборки периодов.

---

## Пункт 6. Инверсный конгруэнтный генератор (ICG)

Здесь **m** — модуль (в ТЗ обозначен буквой n; не путать с объёмом выборки из ЛР №1).

**Xₙ₊₁** при известном Xₙ:

| Условие | Формула |
|---------|---------|
| m ∈ ℙ, Xₙ = 0 | Xₙ₊₁ = b mod m |
| m ∈ ℙ, Xₙ ≠ 0 | Xₙ₊₁ = (a · Xₙ⁻¹ + b) mod m |
| m ∉ ℙ, gcd(a, m) = 1 | Xₙ₊₁ = (a · Xₙ⁻¹ + b) mod m |

**Xₙ⁻¹** — обратный элемент по модулю m (существует, если gcd(Xₙ, m) = 1).

Период и **D(P)** оценивают так же, как в п. 5 (случайные a, b, m из ℤ₂¹² в опытах).

---

## Пункт 7. Генератор Блюма–Блюма–Шуба (BBS)

**Условия:** p, q ∈ ℙ, p ≡ q ≡ 3 (mod 4), M = p·q, gcd(X₀, M) = 1.

**Рекурренция:**

```
Xₙ₊₁ = Xₙ² mod M
```

Младшие биты Xₙ при подходящих p, q ведут себя псевдослучайно. **Период** зависит от X₀ и M; в опытах p, q сэмплируют из **ℤ₂¹⁶**, периоды Pᵢ — из **ℤ₂¹²**, затем считают **D(P)**.

---

## Связь пунктов ЛР №2 с `vismad.py`

| П. ТЗ | Функции |
|-------|---------|
| 1 | `euler_phi_definition`, `euler_phi_factorization`, `factorize`, `euler_phi_dft`, `euler_phi_all` |
| 2 | `fermat_test`, `_distinct_witnesses` |
| 3 | `legendre_symbol`, `jacobi_symbol`, `solovay_strassen_test` |
| 4 | `miller_rabin_test` |
| 5 | `lcg_period`, `sample_periods_lcg` |
| 6 | `icg_next_state`, `icg_period`, `sample_periods_icg` |
| 7 | `bbs_period`, `sample_periods_bbs` |

Вспомогательные: `gcd`, `is_prime`, `mod_inverse`.
